name: "Run Github Script"

on:
  workflow_dispatch:

jobs:
  github-script:
    runs-on: [ ubuntu-latest ]
    steps:
      
      - uses: actions/github-script@v6
        with:
          script: |
            console.log(fetch);
